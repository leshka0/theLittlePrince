clearfix()
	&:before
	&:after
		content: " "
		display: table

	&:after
		clear: both

	*zoom: 1

size($width, $height)
	remify(width, $width)
	remify(height, $height)
	
fullscreen()
	width: 100%
	height: 100%
	
hide()
	visibility: hidden
	opacity: 0